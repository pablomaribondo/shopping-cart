<LinkTo
  data-test-product={{@product.id}}
  @route='item'
  @model={{@product.id}}
  class='product'
>
  <Product::Image @src={{this.productImage}} @toggleZoom={{this.toggleZoom}} />
  <Product::Details @name={{@product.name}} @price={{@product.price}} />
</LinkTo>